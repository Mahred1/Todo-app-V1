function Header() {
    return (
        <header className="header">
           <input className="input" placeholder="Add a new task"></input> 
           <button>+</button>
        </header>
    )
}

export default Header
